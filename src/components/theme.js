import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    fontFamily: 'Roboto, Arial, sans-serif',  // Replace "YourDesiredFont" with your desired font name
  },
});

export default theme;
